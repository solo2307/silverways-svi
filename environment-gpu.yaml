name: silverways-gpu

channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.11.4

  # GPU PyTorch stack
  - pytorch=2.4.1
  - torchvision=0.19.1
  - torchaudio=2.4.1
  - pytorch-cuda=12.1

  # Keep NumPy below 2 for compatibility
  - numpy=1.26.4

  # Core Python/data
  - scipy
  - pip
  - pyyaml
  - setuptools
  - cmake
  - ninja

  # Image processing
  - pillow
  - matplotlib
  - opencv

  - pip:
      # Package/CLI
      - typer==0.17.4
      - rich>=13.0.0
      - tqdm>=4.66.1

      # Model downloads / Hugging Face models
      - huggingface_hub>=0.23.0
      - transformers==4.45.2
      - accelerate>=0.33.0
      - safetensors>=0.4.0

      # YOLO / SAM / SAM2
      - ultralytics>=8.3.237

      # PSPNet / MIT semantic segmentation
      - mit-semseg==1.0.0

      # InternVL GGUF / llama.cpp
      - llama-cpp-python>=0.3.0

      # Optional env variables
      - python-dotenv>=1.0.0