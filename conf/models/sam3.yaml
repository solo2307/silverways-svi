input_dir: data/pano
output_dir: outputs/sam3
recursive: true
limit: null
device: auto

model:
  weights: models/sam3.pt

prompt:
  text:
    - tree
    - grass
    - bush
  conf: 0.25
  imgsz: 1024
  half: false

outputs:
  save_masks: true
  save_json: true
  save_annotated: true