input_dir: data/pano
output_dir: outputs/sam3
recursive: true
limit: null
device: auto

model:
  name_or_path: facebook/sam3

prompt:
  text: vegetation
  detection_threshold: 0.30
  mask_threshold: 0.50

outputs:
  save_masks: true
  save_json: true
