input_dir: data/
# /mnt/sds-hd/sd17f001/ygrin/silverways/ZeroShotEval/mannheim

# data/pano
output_dir: outputs/grounded_sam
recursive: false
limit: 1
device: auto

grounding_dino:
  name_or_path: IDEA-Research/grounding-dino-tiny
  local_dir: models/grounding_dino_tiny

sam2:
  weights: models/sam2_b.pt
  imgsz: 1024

prompt:
  text_labels:
    - sidewalk
    - footway
    - pedestrian walkway
  box_threshold: 0.35
  text_threshold: 0.25

outputs:
  save_json: true
  save_masks: false
  save_annotated: true